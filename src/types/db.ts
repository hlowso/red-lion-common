export interface UserRow {
  id: number;
  username: string;
  email: string;
}

export interface TallyRow {
  id: number;
  gameId: number;
  name: string;
  key: string;
  description: string;
  icon: string;
}
